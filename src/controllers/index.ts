import { StudentController } from './student.controller';
import { TeacherController } from './teacher.controller';

export const controllers = [StudentController, TeacherController];

export { StudentController };
